
export namespace Models {
   export enum colorPicker {
        circleColorPicker,
        customColorPicker
    }
    export interface AuthCredential{
        email:string;
        password:string;
    }
}